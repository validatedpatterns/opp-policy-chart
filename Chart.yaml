apiVersion: v2
description: A Helm chart to serve as the Validated Patterns Template
keywords:
  - pattern
name: opp-policy-chart
version: 0.0.1
dependencies:
  - name: opp-odf
    version: "0.1.0"
    repository: "file://charts/opp-odf"
    condition: odf.enabled